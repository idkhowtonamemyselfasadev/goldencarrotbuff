org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# --- Fabric / Minecraft ------------------------------------------------------
# Pinned to what the server runs, matching the WeaponCooldowns setup so the two
# projects share one warm Loom cache.
minecraft_version=1.21.11
loader_version=0.18.2
fabric_api_version=0.141.3+1.21.11
loom_version=1.17.20

# --- Mod ---------------------------------------------------------------------
mod_version=1.0.0
maven_group=dev.goldencarrot
archives_base_name=goldencarrotbuff
